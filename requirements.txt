# NOTE: `-e .` pulls in the pinned `circuitopt-core` (the compiled Rust core,
# the only compute engine as of v2.0.0). It is built from this repo, not on
# PyPI, so build/install it first with maturin:
#     maturin develop --release -m rust/crates/co-py/Cargo.toml
# (or `pip install rust/crates/co-py`). Then `pip install -r requirements.txt`.
-e .
numpy>=2.0
scipy>=1.10
pytest>=8.0
flask>=3.0
jsonschema>=4.0
